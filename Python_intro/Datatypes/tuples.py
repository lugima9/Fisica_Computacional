########################################################
### Archivo de prueba para aprender sobre las tuplas ###
########################################################

demo_tuple = (1, 2, 3)
months = ("Jan", "Feb", "Mar")
one_elem_tuple = (1,) # Si no se le pone ",", almacena un int

# tuple() -> Función que construye una tupla a partir de un tupla
new_tuple = tuple((1,2,3,4))

# var[n] -> Elemento n-ésimo de una lista (desde el 0)
demo_tuple[3]
demo_tuple[-1]

# del__ -> Elimina la tupla
del months
